.vercel
node_modules
.env
.env*.local

# artefatos do mount (ponte de arquivos) — nunca versionar
.fuse_hidden*

# macOS
.DS_Store

# fontes bibliográficas (PDFs de UpToDate, guidelines, artigos) — material
# licenciado, usado só para curadoria local. O deploy é público: nunca versionar.
*.pdf

# grafo de código (Graphify) — artefato local, regerado a cada indexação
graphify-out/
.env*

# lixo local do mount (rm não funciona; apagar à mão)
_to_delete*/
